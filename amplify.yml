version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - if command -v nvm >/dev/null 2>&1; then nvm install 18 && nvm use 18; else echo "nvm not found"; fi
            - yarn install --frozen-lockfile --production=false
            - HOME=/tmp XDG_CACHE_HOME=/tmp/.cache npx prisma generate
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then yarn db:validate; fi
              fi
        build:
          commands:
            - yarn build
        postBuild:
          commands:
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then
                  if [ -d prisma/migrations ] && [ "$(ls -A prisma/migrations 2>/dev/null)" ]; then
                    HOME=/tmp XDG_CACHE_HOME=/tmp/.cache npx prisma migrate deploy
                  else
                    HOME=/tmp XDG_CACHE_HOME=/tmp/.cache npx prisma db push --skip-generate
                  fi
                fi
              fi
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then yarn db:seed-production; fi
              fi
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*