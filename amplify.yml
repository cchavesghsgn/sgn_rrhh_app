
version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🔧 Selecting Node.js version..."
            - if command -v nvm >/dev/null 2>&1; then nvm install 18 && nvm use 18; else echo "nvm not found, using default Node"; fi
            - echo "📦 Installing dependencies..."
            - yarn install --frozen-lockfile
            - echo "🔍 Validating environment variables (prod only)..."
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then yarn db:validate; fi;
              fi
            - echo "🔧 Generating Prisma client..."
            - npx prisma generate
        build:
          commands:
            - echo "🏗️  Building Next.js application..."
            - yarn build
        postBuild:
          commands:
            - echo "🔄 Applying database changes (prod only)..."
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then
                  if [ -d prisma/migrations ] && [ "$(ls -A prisma/migrations 2>/dev/null)" ]; then
                    echo "Using prisma migrate deploy";
                    npx prisma migrate deploy;
                  else
                    echo "No migrations found, using prisma db push";
                    npx prisma db push --skip-generate;
                  fi
                fi;
              fi
            - echo "🌱 Seeding database (prod only, idempotent)..."
            - |
              if [ "$AMPLIFY_BRANCH" = "master" ] || [ "$AMPLIFY_BRANCH" = "main" ]; then
                if [ "$RUN_DB_TASKS" = "1" ]; then yarn db:seed-production; fi;
              fi
            - echo "✅ Build completed successfully!"
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
    backend:
      phases:
        build:
          commands:
            - echo "ℹ️  No backend build required for Next.js"
